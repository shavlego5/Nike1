import {AuthAccessDirective} from './auth-access.directive';

describe('AuthAccessDirective', () => {
  it('should create an instance', () => {
    const directive = AuthAccessDirective;
    expect(directive).toBeTruthy();
  });
});
